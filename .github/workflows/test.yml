name: Tests

on:
  push:
    branches: [ "main" ]
  pull_request:


jobs:
  django-checks:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version:
          - '3.14'

    services:
      # Label used to access the service container
      postgres:
        # Docker Hub image
        image: postgres:17
        # Provide the password for postgres
        env:
          POSTGRES_USER: symfexit
          POSTGRES_DB: symfexit
          POSTGRES_PASSWORD: symfexit
        # Set health checks to wait until postgres has started
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v6
    - name: Install the latest version of uv and set the Python version
      uses: astral-sh/setup-uv@v7
      with:
        enable-cache: true
        python-version: ${{ matrix.python-version }}
    - name: Run Tests
      run: |
        uv run --frozen python manage.py test
      env:
        DATABASE_URL: postgres://symfexit:symfexit@localhost:5432/symfexit
    - name: Run Django Check
      run: |
        uv run --frozen python manage.py check
    - name: Check migrations
      run: |
        uv run --frozen python manage.py makemigrations --check
      env:
        DATABASE_URL: postgres://symfexit:symfexit@localhost:5432/symfexit
    - name: Check translations
      run: |
        sudo apt install gettext
        uv run --frozen python manage.py makemessages --locale nl --ignore .venv
        if [[ -n $(find symfexit/ -name '*.po' -exec msgattrib --untranslated {} \;) ]]; then exit 1; else echo "No untranslated messages found."; fi
        if [[ -n $(find symfexit/ -name '*.po' -exec msgattrib --only-fuzzy {} \;) ]]; then exit 1; else echo "No fuzzy translations found."; fi
    - name: Run migrate for fixtures
      run: |
        uv run --frozen python manage.py migrate
      env:
        DATABASE_URL: postgres://symfexit:symfexit@localhost:5432/symfexit
    - name: Run command load fixtures
      run: |
        uv run --frozen python manage.py load_fixtures -y
      env:
        DATABASE_URL: postgres://symfexit:symfexit@localhost:5432/symfexit